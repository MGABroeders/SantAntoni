rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TIJDELIJKE TEST RULES - Iedereen heeft toegang
    // Gebruik deze ALLEEN om te testen of Firebase werkt
    // Vervang daarna door de veilige rules uit firestore.rules
    match /{document=**} {
      allow read, write: if true;  // IEDEREEN heeft toegang
    }
  }
}

